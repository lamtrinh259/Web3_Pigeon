const contractAddresses = require("./contractAddresses.json")
const abi = require("./abi.json")

// This index.js file here represents the whole folder
module.exports = {
  abi,
  contractAddresses
}
